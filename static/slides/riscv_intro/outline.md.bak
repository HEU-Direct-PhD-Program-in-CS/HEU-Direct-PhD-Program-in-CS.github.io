# Instruction Set Architecture(ISA)
1. 纵向: `ISA` 在计算机体系结构中的 `layout`. -> What is `ISA`.
2. 横向: 历史上存在过的几种 `ISA` 与现在还在使用的 `ISA`.
3. callback to Compile

## What is ISA

## `ISA` 在计算机体系结构中的 `layout`
- 先思考一个问题
- 我们学过组成原理, 里边的 `ALU`, `Memory`, `Cache` 等都毫无疑问属于计算机硬件.
- 我们也学过编程语言, 形如比较低级的高级语言 - C, 乃至汇编语言, 都毫无疑问属于软件.
- 那么, 我们不禁想问: What is the boundary between computer software and hardware.

### 从 `Software` 看 `ISA`.
任何编程语言最终都要被编译为 机器可以识别的 0/1机器码才能执行, 这句话一方面说明了编译的目标, 另一方面也说明, 任何变成语言都被限制在了 一个框架之中, 一个由 `ISA` 约束的框架之中.  

引用一下 jyy 老师的一句话: `Nothing is magic in computer science.`, 

大部分时候, 我们看 0/1机器码 或是 更 `human-readable` 的汇编语言 的时候, 可能都充满了畏惧, 但事实上, 任何一个 0/1机器码 一定对应着一个计算机的功能. 而这一功能一定是人为约束的. 将 CPU功能映射到0/1码上的 映射关系, 就是 `ISA` 的核心. `OS` 是直接运行在硬件之上的系统软件, 因此 `OS` 的行为也和 `ISA` 的定义息息相关,  